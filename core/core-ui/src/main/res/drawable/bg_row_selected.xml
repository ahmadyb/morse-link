<?xml version="1.0" encoding="utf-8"?>
<!-- Selected state for list rows (Music, Files). A translucent wash of the
     accent rather than a solid block, so the text stays readable and the
     row still reads as part of the list. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/primaryTint" />
</shape>
